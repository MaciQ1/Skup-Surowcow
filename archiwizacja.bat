git init
git config --local user.name "student"
git config --local user.email "student@ur.edu.pl"
git add --all
git commit -m "Projekt"
git archive --format=zip HEAD -o ../127285_AI1_projekt.zip
pause
